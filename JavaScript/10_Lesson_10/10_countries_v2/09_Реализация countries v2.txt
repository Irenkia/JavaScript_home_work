Реализация countries v2

API: REST Countries

при загрузке страницы грузится информация по странам.
https://restcountries.com/v2/all

есть поиск + кнопка “Search”
(я бы переиспользовал это решение:
Input group · Bootstrap v4.6)

при клике на кнопку Search страна ищется не локально (через filter 
искалась ранее), а на сервере
фото 1

и отображается на экране, ну или “Не найдено”
и есть кнопка “Clear Search”, которая очищает input поиска, отсылает 
снова запрос за всеми данными.

обратите внимание, что мы использовали v3 ранее
сейчас - v2, здесь другая структура

отобразить поля:
name, population, area, region

основные моменты:

   -никакого var. let / const
   -arrow functions (стрелочные функции) там, где они нужны
   -map-filter-reduce
   -не тянуть все данные за собой. получили ответ, отсеяли лишние 
    данные (map) и только с нужными полями в каждой стране работаем.
   -таблицы не существует заранее на странице. создать через 
    document.createElement

