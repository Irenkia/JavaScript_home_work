JS - render products from fakestoreapi.com

опис того, що взагалі коїться:

-є відкрите API (ресурс, за яким можна стягнути JSON-дані). 
 стягується 20 товарів.
-в файлі app.js вже робиться fetch на цей ресурс, дані стягнуті, можна 
 подивитися в консолі
-ці дані треба відрендерити (намалювати на сторінці) та зробити 
 функціональність, що буде описана нижче в “пункти завдання”.
-для рендера я використовував bootstrap 4, який вже підключений в 
 index.html. кожний товар робив через bootstrap Card (Cards · Bootstrap)
 + трохи попрацював із картинкою у css, обмеживши висоту (ця частина 
зберіглася в тегу style в index.html). можна зробити не так, можна 
взагалі без бутстрапу. зовнішній вигляд має значення, якщо на це 
залишиться час. в пріорітеті JS частина.

Пункти завдання:

-дані наобхідно відрендерити. 20 товарів стягнуто з сервера → 20 
 товарів має бути на сторінці у мене це виглядає наступним чином:
      фото - 1

-реалізувати пошук за імʼям товару та назвою категорії. (***)

-в кожному обʼєкті товара є більше даних, ніж нам треба. нам треба: 
 title, image, category, price. при отриманні даних відсіяти зайві 
 поля (***).

(***) - використовуючи map/filter/reduce


додаткові завдання “із зірочкою”, кому не вистачить основної частини:

-зробити наповнення htmlStr з html кодом всіх товарів через reduce або 
 map
-при отриманні даних намалювати select як список унікальних категорій 
 (без дублювання option-ів в select-і). через reduce. в мене виглядає 
 наступним чином:
      фото - 2
